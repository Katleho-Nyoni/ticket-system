# TICKET-LOG SYSTEM

<img src="./app.png" />

A tool for an internal team to use to require help with the work device from the IT department.

The frontend will be created using React.js and Tailwind-CSS while the backend with a Node.js package-Express.js.
